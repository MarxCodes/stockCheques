import { MouseoverSearchDirective } from './mouseover-search.directive';

describe('MouseoverSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseoverSearchDirective();
    expect(directive).toBeTruthy();
  });
});
